node_modules
.svelte-kit
build
dist
.DS_Store
.env
.env.*
!.env.example
.vercel
.netlify
.cloudflare

# Editor
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea

# Test artefacts
test-results
playwright-report
playwright/.cache
coverage

# Linter caches — local + CI both write to these. Persisted in CI via
# the actions/cache step in .github/workflows/ci.yml so subsequent runs
# only relint changed files.
.eslintcache
.stylelintcache
node_modules/.cache

# Storybook static build output
storybook-static
