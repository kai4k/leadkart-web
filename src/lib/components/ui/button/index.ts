import Root, {
	type ButtonProps,
	type ButtonVariants,
	type ButtonVariant,
	type ButtonSize,
	buttonVariants
} from './button.svelte';

export {
	Root,
	Root as Button,
	type ButtonProps,
	type ButtonProps as Props,
	type ButtonVariants,
	type ButtonVariant,
	type ButtonSize,
	buttonVariants
};
