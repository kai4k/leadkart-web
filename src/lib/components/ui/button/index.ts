import Root, {
	type ButtonVariants,
	type ButtonVariant,
	type ButtonSize,
	buttonVariants
} from './button.svelte';

export {
	Root,
	Root as Button,
	type ButtonVariants,
	type ButtonVariant,
	type ButtonSize,
	buttonVariants
};
